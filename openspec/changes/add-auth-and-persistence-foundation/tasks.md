## 1. 项目基础迁移

- [ ] 1.1 初始化 Next.js App Router、TypeScript、React 项目结构，并保留现有静态页面可迁移资产。
- [ ] 1.2 配置 `package.json` 脚本，至少包含开发、构建和 lint/类型检查入口。
- [ ] 1.3 将现有页面拆分为 React 页面组件、展示组件和纯数据处理函数。
- [ ] 1.4 保留现有视觉样式，并迁移到 Next.js 可维护的样式组织方式。

## 2. 认证会话

- [ ] 2.1 安装并配置 Supabase Auth 所需依赖和环境变量读取。
- [ ] 2.2 创建 Supabase 浏览器客户端和服务端客户端封装，确保服务端通过 cookie 读取 session。
- [ ] 2.3 实现登录页面或登录组件，支持邮箱密码登录成功和失败反馈。
- [ ] 2.4 实现登出动作，并在登出后清除会话和跳转登录页。
- [ ] 2.5 实现受保护页面 session guard，未登录访问时跳转登录页。

## 3. 数据库与 Prisma

- [ ] 3.1 安装 Prisma，并添加 PostgreSQL 数据库连接配置。
- [ ] 3.2 创建基础数据模型，字段包含 `id`、`user_id`、`occurred_on`、`type`、`content`、`created_at`、`updated_at`。
- [ ] 3.3 添加数据库迁移，并记录迁移失败时的回退方式。
- [ ] 3.4 提供 `.env.example`，列出数据库、Supabase 和部署所需环境变量。

## 4. 用户隔离数据访问

- [ ] 4.1 创建数据访问层，封装列表、新增、删除基础方法。
- [ ] 4.2 确保创建数据时忽略前端传入的身份字段，只使用服务端 session 中的用户身份。
- [ ] 4.3 确保查询数据时按当前 `user_id` 过滤。
- [ ] 4.4 确保删除数据时同时校验记录 ID 和当前 `user_id`。
- [ ] 4.5 对数据输入做基础校验，避免空内容、无效日期或无效类型写入数据库。

## 5. 页面接入

- [ ] 5.1 将记录列表改为读取服务端持久化数据。
- [ ] 5.2 将新增记录表单改为调用服务端写入能力。
- [ ] 5.3 将删除记录按钮改为调用服务端删除能力。
- [ ] 5.4 调整统计、日历和趋势展示，让它们基于服务端返回的数据计算。
- [ ] 5.5 暂时保留本地旧数据导入入口之外的 `localStorage` 数据，不做自动清理。

## 6. 验证与回归

- [ ] 6.1 运行构建验证，确保 `npm run build` 通过。
- [ ] 6.2 人工验证未登录访问受保护页面会跳转登录页。
- [ ] 6.3 人工验证登录后可以新增、查看、删除自己的数据。
- [ ] 6.4 人工验证登出后不能继续访问受保护数据。
- [ ] 6.5 使用两个账号人工验证用户 A 看不到用户 B 的数据。
- [ ] 6.6 对认证守卫、数据访问层或输入校验补充聚焦测试；如果无法补测试，必须在收尾说明原因和风险。

schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
context: |
  Project: 力训记录, a personal strength training log website.
  Current implementation: static HTML/CSS/JavaScript with seed workout data and localStorage persistence.
  Product goal: deployable web app with durable database-backed workout records, check-in calendar, and training progress views.
  Preferred future stack: Next.js, TypeScript, PostgreSQL via Supabase, Prisma, and Vercel.
  Domain notes: workouts are usually grouped as push, pull, legs/core, or mixed sessions.
  Documentation language: all OpenSpec-generated artifacts should be written in Chinese unless the user explicitly asks otherwise.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
rules:
  proposal:
    - 使用中文编写。
  design:
    - 使用中文编写。
  tasks:
    - 使用中文编写。
  spec:
    - 使用中文编写。
